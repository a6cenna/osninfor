#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n,c;cin>>n>>c;
    int a[n+1];
    for(int i=1;i<=n;i++) {cin>>a[i];}

    vector<vector<int>> dp(n+1, vector<int>(n+1, 0));
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
}
