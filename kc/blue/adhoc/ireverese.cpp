#include <bits/stdc++.h>

using namespace std;

void solve() {
    string s;cin>>s;
    int b=0;
    int w=0;
    for(int i=0;i<s.length();i++) {
        if(s[i]=='W')w++;
        else b++;
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    solve();
}
