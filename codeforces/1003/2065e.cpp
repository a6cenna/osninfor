#include <bits/stdc++.h>

using namespace std;

void solve() {
    int n,m,k;cin>>n>>m>>k;
    if(abs(n-m)>k) {
        cout << -1 << '\n';
        return;
    }

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while(t--) {
        solve();
    }
}
